
from typing import List, Dict

LOCAL_MODELS = [""]

def local_model_inference(testing_data: List[Dict], model_name: str) -> List[Dict]:
    # load the model
    pass
    return []

def use_local_model(testing_data: List[Dict], model_name: str) -> List[Dict]:
    """
    Use the local model to detect the anomaly
    """
    pass
    return []